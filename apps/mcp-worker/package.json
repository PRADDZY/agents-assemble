{
  "name": "@agents-assemble/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-assemble/fhir-utils": "workspace:*",
    "@agents-assemble/referral-engine": "workspace:*",
    "@agents-assemble/shared-types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hono": "^4.8.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.87.0"
  }
}
