{
  "name": "@agents-assemble/referral-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-assemble/fhir-utils": "workspace:*",
    "@agents-assemble/shared-types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}

